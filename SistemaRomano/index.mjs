import { romanNumbersAsociation, romanNumberValidator } from "./utils.mjs";

function romanToArabicConverter(romanNumber) {
  if (romanNumberValidator(romanNumber)) {
  } else {
    return "Por favor, introduce número romano válido";
  }
}

function arabicToRomanConverter(arabicNumber) {
  if (number >= 1 && number <= 3999) {
  } else {
    return "Por favor, introduce un valor entre 1 y 3999";
  }
}
